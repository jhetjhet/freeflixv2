services:
  frontend:
    build:
      context: ./freeflix-react
      target: build
    env_file:
      - ./freeflix-react/.env.development
    volumes:
      - ./freeflix-react:/app
      - /app/node_modules
    develop:
      watch:
        - action: rebuild
          path: ./freeflix-react/package.json
    stdin_open: true
    tty: true
    command: ["npm", "start"]
  
  backend:
    develop:
      watch:
        - action: sync
          path: .
          initial_sync: true
          target: /app
    command: python manage.py runserver 0.0.0.0:8000

  node_backend:
    environment:
      - ENV_MODE=development
    volumes:
      - ./node-media-server:/app
      - /app/node_modules
    develop:
      watch:
        - action: rebuild
          path: ./node-media-server/package.json
    command: npm start

  web-server:
    volumes:
      - ./nginx-server/config.dev.conf:/etc/nginx/conf.d/default.conf